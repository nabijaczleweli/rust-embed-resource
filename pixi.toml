[workspace]
channels = ["https://prefix.dev/conda-forge"]
platforms = ["win-64", "win-arm64"]
preview = ["pixi-build"]

[package.build]
backend = { name = "pixi-build-rust", version = "*" }

[tasks]
build = { cmd = "pixi build -o out" }
